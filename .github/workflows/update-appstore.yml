name: Update App Store KR Top 10

on:
  schedule:
    - cron: "0 23 * * *"   # 매일 08:00 KST (UTC 23:00, UTC 23시)
  workflow_dispatch:       # 수동 실행 버튼

permissions:
  contents: write          # 리포지토리에 푸시 권한

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Run update script
        run: python scripts/update_appstore_kr.py

      - name: Commit & Push JSON
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/appstore-kr.json
          git commit -m "auto: update appstore KR top10" || echo "No changes"
          git push
